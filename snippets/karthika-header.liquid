{% doc %}
  Karthika Supermarket — Redesigned Mobile Header
  Layout: Single horizontal row — Logo (left) | Search (center, flexible) | Cart (right)
  Below header: compact delivery location strip
{% enddoc %}

{%- liquid
  assign cart_count = cart.item_count
-%}

<header class="karthika-mobile-header" role="banner">
  <div class="karthika-header-row">

    <!-- Logo (Left) -->
    <a href="{{ routes.root_url }}" class="karthika-header-logo" aria-label="{{ shop.name }}">
      {%- if settings.logo != blank -%}
        <img
          src="{{ settings.logo | image_url: width: 160 }}"
          alt="{{ shop.name | escape }}"
          class="karthika-header-logo-img"
          width="80"
          height="36"
          loading="eager"
        >
      {%- else -%}
        <div class="karthika-header-logo-text">
          <svg class="karthika-logo-icon" viewBox="0 0 28 28" fill="none" aria-hidden="true">
            <rect width="28" height="28" rx="8" fill="#B40000"/>
            <path d="M7 10h4l2 5 2-7 2 5h4" stroke="#FFD200" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <circle cx="21" cy="19" r="2" fill="#FFD200"/>
            <circle cx="10" cy="19" r="2" fill="#FFD200"/>
            <path d="M6 8h16" stroke="#FFD200" stroke-width="1.5" stroke-linecap="round"/>
          </svg>
          <div class="karthika-logo-wordmark">
            <span class="karthika-logo-name">Karthika</span>
            <span class="karthika-logo-tagline">Supermarket</span>
          </div>
        </div>
      {%- endif -%}
    </a>

    <!-- Search Bar (Centre, flexible) -->
    <div
      class="karthika-header-search karthika-search-bar-trigger"
      role="search"
      tabindex="0"
      aria-label="Search groceries"
    >
      <svg class="karthika-search-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <circle cx="11" cy="11" r="8"></circle>
        <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
      </svg>
      <span class="karthika-search-placeholder">Search for products...</span>
    </div>

    <!-- Cart Icon (Right) -->
    <a
      href="{{ routes.cart_url }}"
      class="karthika-header-cart"
      aria-label="Cart ({{ cart_count }} items)"
      id="karthika-cart-icon-header"
    >
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
        <circle cx="9" cy="21" r="1"></circle>
        <circle cx="20" cy="21" r="1"></circle>
        <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
      </svg>
      {%- if cart_count > 0 -%}
        <span class="karthika-cart-badge" aria-label="{{ cart_count }} items in cart">
          {{- cart_count | at_most: 99 -}}
        </span>
      {%- endif -%}
    </a>

  </div>
</header>

<!-- Delivery Location Strip -->
<div class="karthika-delivery-strip karthika-change-location-btn" role="button" tabindex="0" aria-label="Change delivery location">
  <svg class="karthika-delivery-pin" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
    <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/>
  </svg>
  <span class="karthika-delivery-label">Deliver to:</span>
  <span class="karthika-delivery-address">Tampines Central, Singapore</span>
  <svg class="karthika-delivery-caret" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
    <polyline points="6 9 12 15 18 9"></polyline>
  </svg>
</div>
